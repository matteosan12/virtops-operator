package winrm

// Package winrm will implement the executor for guest operations on Windows (WinRM):
// - Administrator password rotation.
// - Access validation.
// - (Future) integration with LAPS/AD when available.

// Placeholder for build.

type Options struct{}

func RotatePassword(opts Options) error { return nil }
