apiVersion: apps/v1
kind: Deployment
metadata:
  name: virtops-operator
  namespace: virt-ops
  labels:
    app: virtops-operator
spec:
  replicas: 1
  selector:
    matchLabels:
      app: virtops-operator
  template:
    metadata:
      labels:
        app: virtops-operator
    spec:
      serviceAccountName: virtops-operator
      containers:
        - name: manager
          image: image-registry.openshift-image-registry.svc:5000/virt-ops/virtops-operator
          imagePullPolicy: IfNotPresent
          command:
            - /manager
          args:
            - --leader-elect
          env:
            - name: SSH_EXECUTOR_IMAGE
              value: "alpine:3.19"
            - name: WINRM_EXECUTOR_IMAGE
              value: "image-registry.openshift-image-registry.svc:5000/virt-ops/virtops-winrm-executor:latest"
          ports:
            - containerPort: 8080
              name: metrics
            - containerPort: 8081
              name: healthz
          livenessProbe:
            httpGet:
              path: /healthz
              port: 8081
            initialDelaySeconds: 10
            periodSeconds: 10
          readinessProbe:
            httpGet:
              path: /readyz
              port: 8081
            initialDelaySeconds: 10
            periodSeconds: 10
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            runAsNonRoot: true
      securityContext: {}
